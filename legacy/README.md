# Legacy code

This folder holds retired entrypoints kept for reference only.

| File | Description |
|------|-------------|
| `idle_champions_automation_tk.py` | Original tkinter automation GUI (superseded by the PySide6 app in `ic_ui/`) |

**Use `app_launcher.py` at the repo root** to start the current application.
